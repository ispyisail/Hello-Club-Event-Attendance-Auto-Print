# =============================================================================
# Hello Club Event Attendance Auto-Print - Environment Configuration
# =============================================================================
# This file contains all environment variables needed for the application.
# Copy this file to .env and fill in your actual values.
# NEVER commit your .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Hello Club API Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Your Hello Club API authentication key
# Get this from your Hello Club account settings
API_KEY=

# Optional: Override the default API endpoint
# Only change this if Hello Club provides a different endpoint
# API_BASE_URL=https://api.helloclub.com

# Optional: API request timeout in milliseconds (default: 30000 = 30 seconds)
# Increase this if you have a slow internet connection or the API is slow to respond
# API_TIMEOUT=30000

# -----------------------------------------------------------------------------
# Email Printing Configuration (Optional - only needed for email print mode)
# -----------------------------------------------------------------------------
# Note: All email configuration has been moved from config.json to environment
# variables for better security. Email credentials should NEVER be in config.json

# Email address of the network printer
# Example: printer@company.com
PRINTER_EMAIL=

# SMTP Server Settings
# For Gmail, use the settings below and enable "App Passwords" in your Google Account
# For other providers, consult their SMTP documentation
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# SMTP Authentication
# IMPORTANT: Use app-specific passwords, NOT your main email password
#
# === HOW TO CREATE A GOOGLE APP PASSWORD ===
# 1. Go to https://myaccount.google.com/ → Click "Security"
# 2. Enable "2-Step Verification" if not already enabled
# 3. Scroll to "2-Step Verification" → Click it → Scroll to bottom
# 4. Click "App passwords" (you may need to sign in again)
# 5. Select "Mail" as app, "Other" as device, enter "Hello Club Printer"
# 6. Click "Generate" - you'll see a 16-character password like: uwmo ybdz apyk pmfv
# 7. Copy-paste it below (spaces are OK - they're automatically removed!)
#
# For Outlook: https://account.live.com/proofs/AppPassword
SMTP_USER=
SMTP_PASS=

# Email address to send from (defaults to SMTP_USER if not specified)
EMAIL_FROM=

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Set to 'production' to disable debug logging and enable optimizations
# Set to 'development' for verbose logging and easier debugging
NODE_ENV=development
